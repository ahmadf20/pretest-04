pointer p;
    // List First;
    // int count = 5;

    // createList(First);
    // for(int i = 0; i < count; i++)
    // {
    //     createElemt(p);
    //     insertSortNPM(First, p);
    // }

    // traversal(First);